---
---

<section class="hero">
  <div class="container hero-content">
    <p class="hero-pretitle">Backend Developer</p>
    <h1 class="hero-title">Sergio <span class="accent">Ávalos</span></h1>
    <p class="hero-subtitle">Lima, Perú · Backend Developer · Estudiante de Ing. de Sistemas</p>
    <p class="hero-bio">
      Backend Developer con experiencia en arquitectura de sistemas, optimización de performance
      y desarrollo guiado por IA. He trabajado en plataformas de automatización, e-commerce y
      sistemas educativos, aplicando Clean Architecture, CQRS y CI/CD en producción.
    </p>
    <div class="hero-actions">
      <a href="/projects" class="btn btn-primary btn-lg">Ver proyectos</a>
      <a href="/about" class="btn btn-secondary btn-lg">Sobre mí</a>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: auto;
    display: flex;
    align-items: center;
    padding-block: var(--space-16);
  }

  .hero-content {
    max-width: var(--content-width);
  }

  .hero-pretitle {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    letter-spacing: var(--tracking-widest);
    text-transform: uppercase;
    color: var(--color-accent-text);
    margin-bottom: var(--space-4);
  }

  .hero-title {
    font-size: clamp(var(--text-3xl), 8vw, var(--text-4xl));
    font-weight: 700;
    letter-spacing: var(--tracking-tight);
    margin-bottom: var(--space-2);
  }

  .hero-title .accent {
    color: var(--color-accent-text);
  }

  .hero-subtitle {
    font-size: var(--text-lg);
    color: var(--color-text-muted);
    margin-bottom: var(--space-6);
    line-height: var(--leading-normal);
  }

  .hero-bio {
    font-size: var(--text-base);
    color: var(--color-text-muted);
    max-width: 520px;
    margin-bottom: var(--space-8);
    line-height: var(--leading-loose);
  }

  .hero-actions {
    display: flex;
    flex-direction: column;
    flex-wrap: wrap;
    gap: var(--space-4);
  }

  .hero-actions .btn {
    width: 100%;
    justify-content: center;
  }

  @media (min-width: 640px) {
    .hero {
      padding-block: var(--space-24);
      min-height: calc(100vh - var(--nav-height));
    }

    .hero-actions {
      flex-direction: row;
    }

    .hero-actions .btn {
      width: auto;
    }
  }
</style>