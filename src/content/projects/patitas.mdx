---
title: "Patitas — Adopción de Mascotas"
description: "Plataforma full-stack para adopción de mascotas con sistema de matchmaking. Estuvo en producción."
stack: ["TypeScript", "React", "Vite", "Express", "PostgreSQL", "Prisma", "JWT"]
year: 2023
featured: false
order: 4
repoUrl: "https://github.com/Spai26/patitas"
---

## El proyecto

Plataforma que conecta adoptantes con refugios. El diferencial era un sistema de matching basado en estilo de vida, vivienda y experiencia del adoptante.

## Sistema de matching

```typescript
interface MatchingCriteria {
  activityLevel: 'low' | 'medium' | 'high';
  experienceWithPets: 'none' | 'some' | 'experienced';
  housingType: 'apartment' | 'house' | 'large';
  hasYard: boolean;
  hoursAlonePerDay: number;
  otherPets: Pet[];
}

function calculateCompatibility(
  adopter: MatchingCriteria,
  pet: PetProfile
): number {
  let score = 50; // base

  if (adopter.activityLevel === pet.activityLevel) score += 20;
  if (adopter.housingType === pet.minHousingType) score += 15;
  if (!adopter.hasYard && pet.requiresYard) score -= 30;

  return Math.max(0, Math.min(100, score));
}
```

## Stack

| Capa | Tecnología |
|------|------------|
| Frontend | React + Vite |
| Backend | Express + TypeScript |
| Database | PostgreSQL + Prisma |
| Auth | JWT |
| Hosting | Railway |

## Lo que aprendí

Fue mi primer full-stack completo solo y aprendí que mantener la separación frontend/backend es más difícil cuando sos la única persona tocando todo. También que el matching no es ciencia exacta — requirió iterar con feedback real de refugios.
